// No markdown content collections yet — the skills catalogue is a typed data
// file (src/data/skills.ts), not a glob of markdown. Add a collection here if a
// future section (e.g. a blog) needs markdown-authored pages.
export const collections = {};
